services:
  db:
    image: postgres:15
    container_name: book_review_db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: book_review_db
    ports:
      - "5432:5432"
